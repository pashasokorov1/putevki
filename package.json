{
  "name": "fleet-fuel-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @fleet/web",
    "dev:api": "npm run dev --workspace @fleet/api",
    "build:web": "npm run build --workspace @fleet/web",
    "build:api": "npm run build --workspace @fleet/api",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspace @fleet/domain"
  }
}
